name: "Run CI"

on: pull_request

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: setup-pixi
        uses: prefix-dev/setup-pixi@v0.4.1    
      - run: pixi run test
